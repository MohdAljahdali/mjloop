# mjloop

> دورات تطوير مُتحقَّق منها لـ Claude Code.

[![إضافة Claude Code](https://img.shields.io/badge/Claude_Code-plugin-6B5CE7?style=flat-square)](https://docs.anthropic.com/en/docs/claude-code)
[![Node.js 20+](https://img.shields.io/badge/Node.js-20%2B-339933?style=flat-square&logo=nodedotjs&logoColor=white)](https://nodejs.org/)
[![TypeScript](https://img.shields.io/badge/TypeScript-5-3178C6?style=flat-square&logo=typescript&logoColor=white)](https://www.typescriptlang.org/)

[English](README.md) · **العربية** · [简体中文](README.zh-CN.md) · [Español](README.es.md) · [Português (Brasil)](README.pt-BR.md) · [Français](README.fr.md) · [Deutsch](README.de.md) · [日本語](README.ja.md) · [한국어](README.ko.md) · [Русский](README.ru.md) · [हिन्दी](README.hi.md) · [Bahasa Indonesia](README.id.md) · [Türkçe](README.tr.md) · [Tiếng Việt](README.vi.md)

**اجعل وكلاء البرمجة يثبتون أنهم أنهوا العمل.**

`mjloop` إضافة لـ Claude Code تحوّل عمل الوكلاء إلى دورات محدودة ومدعومة بالأدلة.
يختار القائد الوكلاء المناسبين للمهمة، ويشغّلهم في سياقات معزولة، ولا يقبل النجاح إلا
بعد أن يسجّل المحرّك نتيجة أوامر التحقق الحقيقية الخاصة بمشروعك.

`الطلب ← المسار ← وكلاء معزولون ← تحقق المحرّك ← نتيجة مثبتة بالدليل`

> [!IMPORTANT]
> يدعم `mjloop` حاليًا Claude Code. محوّلات وكلاء البرمجة الآخرين ليست ضمن الإصدار
> المنشور حتى الآن.

## لماذا mjloop؟

- **الدليل بدل الثقة** — لا يستطيع ادعاء النجاح تجاوز إيصال فاشل أو مفقود من المحرّك.
- **حالة لا يستطيع الوكلاء إعادة كتابتها** — يملك خادم MCP حالة التشغيل والملفات المشتقة.
- **استقلالية محدودة** — توقف حواجز الدورات والركود والخطأ المتكرر العمل الذي لا يتقدم.
- **مسار مناسب لكل مهمة** — تعديل قصير، أو بناء متعدد الدورات، أو إصلاح يبدأ بإعادة
  الإنتاج، أو تخطيط خاضع للمراجعة.

## البدء السريع

تحتاج إلى Claude Code وNode.js 20 أو أحدث وGit.

```bash
git clone https://github.com/MohdAljahdali/mjloop.git
cd mjloop/engine
npm install
npm run build
cd ..
claude plugin marketplace add "$PWD"
claude plugin install mjloop@mjloop
```

بعد ذلك افتح Claude Code داخل مشروع ونفّذ:

```text
/mjloop:init
/mjloop:edit أضف التحقق من مدخلات نموذج التسجيل
```

> [!NOTE]
> يجب بناء النسخة الجديدة مرة واحدة لأن خادم MCP وأداة الخطافات يعملان من
> `engine/dist/`. راجع [دليل التثبيت الكامل](docs/install.ar.md) للتحقق والتحديث وحل
> المشكلات.

## اختر المسار المناسب

| الأمر | مناسب لـ | القاعدة المضمّنة |
|---|---|---|
| `/mjloop:edit <الطلب>` | التغييرات الصغيرة المحددة | دورة واحدة، ويُصعّد العمل إذا اتسع النطاق |
| `/mjloop:build <الهدف>` | الميزات والتنفيذ الأكبر | دورات متحققة متكررة حتى الاكتمال أو التوقف |
| `/mjloop:fix <المشكلة>` | الأعطال والتراجعات | إعادة إنتاج العطل قبل قبول الإصلاح |
| `/mjloop:plan <الفكرة>` | تحويل الفكرة إلى قصص قابلة للبناء | فحص الملاءمة والموافقة قبل إنشاء القصص |

استخدم `/mjloop:status` لعرض التشغيل الحالي، و`/mjloop:resume` لمتابعة تشغيل متوقف،
و`/mjloop:stop` لإنهائه، و`/mjloop:web` لفتح لوحة التحكم في المتصفح.

## اختر مقدار المراجعة التي يدفع ثمنها التشغيل

إعداد واحد يحدد مقدار المراجعة في كل تشغيل، وقيمه ثلاث لا رابع لها:

| الوضع | ما يضيفه | مناسب لـ |
|---|---|---|
| `economy` | الفحوص الحتمية أولًا، ولا يُستدعى مختص إلا حين تعجز عن إغلاق بُعد | التغييرات المحدودة في شيفرة مألوفة |
| `adaptive` | مراجعة مستقلة حين تطلبها إشارات الخطر في التشغيل نفسه | أغلب العمل، وهو ما يبدأ به المشروع الجديد |
| `strict` | مراجعة مستقلة للخطة، وتحقق مستقل بعد التنفيذ، وكل مختص قابل للتطبيق | الترحيلات والأسطح الأمنية وكل ما يكلف الخطأ فيه كثيرًا |

```text
/mjloop:config set orchestration.quality.mode adaptive
```

**يغيّر الوضع طريقة الوصول إلى الدليل، لا تعريف الإنجاز.** الأوضاع الثلاثة تغلق الأبعاد
المطلوبة نفسها، ولا يجوز لأيها إعلان النجاح بدليل أضعف. وحين يوجد أكثر من طريق لإغلاق
الأبعاد نفسها، يفضّل المحرّك ما يستهلك الرموز أقل، ثم يفصل التساوي بالتكلفة ثم بالوقت.

- **يعمل كل تشغيل على سياسة مثبّتة.** الوضع والميزانية والوكلاء المسموح إرسالهم تُجمّد عند
  بداية التشغيل، فتعديل الإعداد أثناءه يغيّر التشغيل التالي لا الحالي.
- **المشاريع القائمة ليست محكومة بهذا.** المشروع الذي لم يسمِّ وضعًا قط يحتفظ بسلوكه
  السابق حتى يختار الانضمام بتعيين وضع صراحةً.
- **التشغيل الذي يحتاج قرار شخص يتوقف بلا استهلاك.** `waiting_for_user` ينتظر قرارًا في
  عملية تدميرية، و`budget_exhausted` ينتظر تعديل ميزانية صريحًا لذلك التشغيل وحده. ولا
  يستهلك التشغيل الموقوف وكيلًا ولا إرسالًا ولا رمزًا واحدًا أثناء الانتظار، ويكمل
  `/mjloop:resume` من الموضع نفسه بعد تسجيل القرار في لوحة التحكم.
- **العمليات التدميرية تُمنع قبل وقوعها.** حذف جدول أو ميزة أو ترحيل غير قابل للعكس: يتوقف
  التشغيل أمامه ويسأل. والقرار للمشغّل، يُتخذ في لوحة التحكم ومرتبط بتلك العملية بعينها —
  لا وكيل ولا أداة MCP تستطيع اتخاذه.
- **لا شيء يُدمج أو يُنشر نيابة عنك.** التشغيل الناجح يودع عمله المتحقق منه ويقف عند ذلك،
  ويبقى الدمج والنشر قرارين بشريين في كل الأوضاع.

تحمل أرقام الرموز والتكلفة والوقت الوسم الذي استحقته من المحرّك — `measured` أو
`estimated` أو `unavailable` — والمشروع الذي لا يملك جدول تسعير يُقال له إن التكلفة
`unavailable` بدل أن يُعرض عليه رقم لم يقسه أحد. تجد صيغ السجلات ومصفوفة السلوك الكاملة في
[وثيقة التصميم](docs/superpowers/specs/2026-08-04-mjloop-quality-cycle-modes-design.md).

## ماذا يحدث في الدورة؟

1. يركّب القائد طاقمًا من المسار المختار ويسجّل سبب ضم كل مختص اختياري أو استبعاده.
2. يعمل الوكلاء المقيّدون بعقود في سياقات معزولة ومسؤوليات محددة.
3. يشغّل المحرّك أوامر التحقق المثبتة عند بداية التشغيل ويحفظ السجل الكامل خارج رواية الوكيل.
4. تصبح نتيجة التحقق الفاشلة مدخلًا للدورة التالية، ويمكن لإيصال ناجح إغلاق التشغيل.
5. توقف حواجز الأمان الدورات عند بلوغ الحد أو الركود أو تكرار الخطأ نفسه.

## أكثر من مجرد حلقة تنفيذ

- **استكشاف الميزة** — تجري مهارة `mjloop-feature-discovery` مقابلة بقرار واحد كل مرة
  وتتوقف عند موجز يمكن لشخص مراجعته والموافقة عليه.
- **توجيه واعٍ بالمشروع** — خرائط المكونات والمهارات المقبولة توجه الأدوار دون تغيير
  تشغيل جارٍ.
- **لوحة تحكم في المتصفح** — افحص التشغيل والخطط والقصص والأدلة والإعدادات والذاكرة عبر
  `/mjloop:web`.
- **مسارات قابلة للتوسعة** — أضف وكيلًا أو مهارة أو مسارًا باستخدام `/mjloop:add`.

> [!TIP]
> ابدأ بـ `/mjloop:edit` على تغيير حقيقي ومحدود؛ فهو أسرع طريقة لرؤية عقد التحقق دون
> تكلفة تشغيل متعدد الدورات.

## تابع القراءة

- [لماذا بُني mjloop](docs/about.ar.md)
- [التثبيت وحل المشكلات](docs/install.ar.md)
- [الأوامر والإعدادات ومسارات العمل](docs/usage.ar.md)
- [التوثيق الإنجليزي](docs/about.md)

إذا كان `mjloop` يحل مشكلة تعرفها، ففكّر في إضافة نجمة للمستودع كي يتمكن مطورون آخرون
من العثور عليه.
